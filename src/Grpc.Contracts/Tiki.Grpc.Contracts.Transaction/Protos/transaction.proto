syntax = "proto3";

option csharp_namespace = "Tiki.Grpc.Contracts.Transaction";

package tiki.grpc.contracts.transaction;

// Placeholder contract — real RPCs to follow. Owning service: Transaction.
service TransactionService {
  rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {
}

message PingResponse {
  bool ok = 1;
}
